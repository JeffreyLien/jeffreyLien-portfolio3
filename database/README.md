# Generated analytical warehouse

`institutional_research.db` is generated locally by `notebooks/03_build_sqlite_warehouse.ipynb` and is intentionally not committed to keep the public portfolio lightweight.

Run notebooks `01` through `03` in order to recreate the SQLite warehouse from the synthetic source data.
