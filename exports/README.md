# Reporting exports

This folder keeps compact aggregate outputs for program KPIs, cohort retention, course performance, and the validated external-submission extract.

Large student-level and response-level exports are generated locally by `08_build_reporting_exports.ipynb` rather than committed to the public repository.
